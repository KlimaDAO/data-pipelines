apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

namePrefix: prod-

commonLabels:
  bot: data-pipelines

patchesStrategicMerge:
  - deployment_set_environment.yaml
